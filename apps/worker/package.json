{
  "name": "@forgeai/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/worker.ts",
    "lint": "eslint .",
    "start": "node dist/worker.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@forgeai/config": "workspace:*",
    "@forgeai/logger": "workspace:*",
    "@forgeai/workflow": "workspace:*",
    "bullmq": "^5.34.5",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
